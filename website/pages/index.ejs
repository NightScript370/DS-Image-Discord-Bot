<!DOCTYPE html>
<html>
  <% var subtitle = "Home" %>
  <% include ../parts/head %>
  <!-- Start content -->
  
  <div class="jumbotron text-center">
    <h1 class="botDisplay">Introducing <%= client.user.username %></h1>
    <p class="botLead">
      <%= client.user.username %> is an all-in-one Discord bot dedicated to helping modding communities and more.<br>
      It can fulfill your server's moderation needs and create fun events for your community to enjoy
    </p>
  </div>
  
      <div class=container>
        
        <% const features = [
          {
            icon: "level-up",
            text: "Leveling system with configurable point multipliers"
          },
          {
            icon: "cogs",
            text: "Powerful per-server settings system"
          },
          {
            icon: "gamepad",
            text: "Tons of fun games"
          },
          {
            icon: "star",
            text: "Starboard system (if you're into that thing)"
          },
          {
            icon: "key",
            text: "Strong Server Management Tools"
          },
          {
            icon: "file-code-o",
            text: "New features added regularly"
          }
        ] %>
        
        <h2 class="text-center" id="featuresHeader">Features</h2>
        <br />

        <div class="row">
          
          <% for (feature of features) { %>
          <div class="col-<%= feature.col || 4 %> text-center">
            <div class="hoverable">
              <h2>
                <i class="fa fa-<%= feature.icon %>"></i>
              </h2>
              <p><%= feature.text %></p>
            </div>
          </div>
          <% } %>
        </div>
      </div>

  <!-- End content -->
  <% include ../parts/footer %>
</html>